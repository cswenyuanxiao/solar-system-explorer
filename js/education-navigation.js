// Education Navigation System class EducationNavigation { constructor() { this.currentSection = 'quiz'; this.init(); } init() { this.setupNavigation(); this.bindEvents(); } setupNavigation() { const educationNav = document.querySelector('.education-nav'); if (!educationNav) return; // Add navigation styles educationNav.style.cssText = ` display: flex; justify-content: center; gap: 1rem; margin: 2rem 0; flex-wrap: wrap; `; // Style navigation buttons document.querySelectorAll('.edu-nav-btn').forEach(btn => { btn.style.cssText = ` padding: 0.8rem 1.5rem; border: none; background: var(--card-background); color: var(--text-color); border-radius: 25px; cursor: pointer; font-size: 1rem; transition: all 0.3s ease; backdrop-filter: blur(10px); -webkit-backdrop-filter: blur(10px); border: 1px solid var(--border-color); font-weight: 500; `; }); } bindEvents() { document.addEventListener('click', (e) => { if (e.target.classList.contains('edu-nav-btn')) { const section = e.target.getAttribute('data-section'); this.switchSection(section); } }); } switchSection(section) { // Update navigation buttons document.querySelectorAll('.edu-nav-btn').forEach(btn => { btn.classList.remove('active'); btn.style.background = 'var(--card-background)'; btn.style.color = 'var(--text-color)'; }); const activeBtn = document.querySelector(`[data-section="${section}"]`); if (activeBtn) { activeBtn.classList.add('active'); activeBtn.style.background = 'var(--primary-color)'; activeBtn.style.color = '#000'; } // Update sections document.querySelectorAll('.education-section').forEach(sectionEl => { sectionEl.classList.remove('active'); sectionEl.style.display = 'none'; }); const targetSection = document.getElementById(`${section}Section`); if (targetSection) { targetSection.classList.add('active'); targetSection.style.display = 'block'; } this.currentSection = section; // Initialize section-specific features this.initializeSection(section); } initializeSection(section) { switch (section) { case 'quiz': this.initializeQuiz(); break; case 'lessons': this.initializeLessons(); break; case 'progress': this.initializeProgress(); break; } } initializeQuiz() { // Quiz is already initialized by quiz-system.js // Just ensure it's visible and working if (window.quizSystem) { // Refresh quiz if needed window.quizSystem.updateNavigationButtons(); } } initializeLessons() { // Lessons are already initialized by astronomy-module.js // Just ensure it's visible and working if (window.astronomyModule) { // Refresh module if needed window.astronomyModule.updateProgressDisplay(); } } initializeProgress() { // Progress is already initialized by progress-tracker.js // Just ensure it's visible and working if (window.progressTracker) { // Refresh progress display window.progressTracker.updateAllProgress(); } } // Add section-specific animations addSectionAnimations() { const sections = document.querySelectorAll('.education-section'); sections.forEach(section => { section.style.opacity = '0'; section.style.transform = 'translateY(20px)'; section.style.transition = 'opacity 0.5s ease, transform 0.5s ease'; }); // Animate active section const activeSection = document.querySelector('.education-section.active'); if (activeSection) { setTimeout(() => { activeSection.style.opacity = '1'; activeSection.style.transform = 'translateY(0)'; }, 100); } } // Track section usage for analytics trackSectionUsage(section) { if (window.progressTracker) { // Add points for exploring different sections window.progressTracker.progress.totalPoints += 1; window.progressTracker.saveProgress(); } } // Add keyboard navigation setupKeyboardNavigation() { document.addEventListener('keydown', (e) => { const navButtons = document.querySelectorAll('.edu-nav-btn'); const currentIndex = Array.from(navButtons).findIndex(btn => btn.classList.contains('active') ); switch (e.key) { case 'ArrowLeft': e.preventDefault(); const prevIndex = (currentIndex - 1 + navButtons.length) % navButtons.length; navButtons[prevIndex].click(); break; case 'ArrowRight': e.preventDefault(); const nextIndex = (currentIndex + 1) % navButtons.length; navButtons[nextIndex].click(); break; case '1': e.preventDefault(); document.querySelector('[data-section="quiz"]').click(); break; case '2': e.preventDefault(); document.querySelector('[data-section="lessons"]').click(); break; case '3': e.preventDefault(); document.querySelector('[data-section="progress"]').click(); break; } }); } // Add accessibility features setupAccessibility() { const navButtons = document.querySelectorAll('.edu-nav-btn'); navButtons.forEach((button, index) => { button.setAttribute('aria-label', `Switch to ${button.textContent} section`); button.setAttribute('role', 'tab'); button.setAttribute('aria-selected', button.classList.contains('active')); button.setAttribute('tabindex', button.classList.contains('active') ? '0' : '-1'); }); // Add focus management navButtons.forEach(button => { button.addEventListener('focus', () => { button.setAttribute('aria-selected', 'true'); }); button.addEventListener('blur', () => { button.setAttribute('aria-selected', 'false'); }); }); } // Add section transition effects addTransitionEffects() { const sections = document.querySelectorAll('.education-section'); sections.forEach(section => { section.addEventListener('transitionend', () => { if (section.classList.contains('active')) { section.style.transform = 'none'; } }); }); } // Initialize all features setupAllFeatures() { this.setupKeyboardNavigation(); this.setupAccessibility(); this.addTransitionEffects(); } } // Initialize education navigation when DOM is ready document.addEventListener('DOMContentLoaded', () => { window.educationNavigation = new EducationNavigation(); // Setup all features after a short delay setTimeout(() => { if (window.educationNavigation) { window.educationNavigation.setupAllFeatures(); } }, 500); });