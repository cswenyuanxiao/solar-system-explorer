// Quiz System for Solar System Education class QuizSystem { constructor() { this.questions = this.loadQuestions(); this.currentQuestion = 0; this.score = 0; this.userAnswers = []; this.quizCompleted = false; this.init(); } init() { this.setupQuizUI(); this.bindEvents(); } loadQuestions() { return [ // Basic Planet Knowledge { id: 1, type: 'multiple-choice', category: 'basic', difficulty: 'easy', question: 'Which planet is closest to the Sun?', options: ['Venus', 'Mercury', 'Earth', 'Mars'], correct: 1, explanation: 'Mercury is the closest planet to the Sun, with an average distance of 57.9 million kilometers.', points: 10 }, { id: 2, type: 'multiple-choice', category: 'basic', difficulty: 'easy', question: 'Which planet is known as the "Red Planet"?', options: ['Venus', 'Mars', 'Jupiter', 'Saturn'], correct: 1, explanation: 'Mars is called the "Red Planet" due to its reddish appearance caused by iron oxide on its surface.', points: 10 }, { id: 3, type: 'multiple-choice', category: 'basic', difficulty: 'medium', question: 'Which planet has the most moons?', options: ['Jupiter', 'Saturn', 'Uranus', 'Neptune'], correct: 1, explanation: 'Saturn has 146 confirmed moons, making it the planet with the most satellites in our solar system.', points: 15 }, { id: 4, type: 'true-false', category: 'basic', difficulty: 'easy', question: 'Earth is the only planet in our solar system known to have life.', options: ['True', 'False'], correct: 0, explanation: 'True! Earth is the only planet where life has been confirmed to exist.', points: 10 }, { id: 5, type: 'multiple-choice', category: 'advanced', difficulty: 'hard', question: 'Which planet has the highest surface temperature?', options: ['Mercury', 'Venus', 'Earth', 'Mars'], correct: 1, explanation: 'Venus has the highest surface temperature, averaging 462°C due to its thick greenhouse atmosphere.', points: 20 }, { id: 6, type: 'multiple-choice', category: 'advanced', difficulty: 'medium', question: 'What is the largest planet in our solar system?', options: ['Saturn', 'Jupiter', 'Uranus', 'Neptune'], correct: 1, explanation: 'Jupiter is the largest planet, with a diameter of 139,822 kilometers.', points: 15 }, { id: 7, type: 'multiple-choice', category: 'advanced', difficulty: 'hard', question: 'Which planet rotates on its side?', options: ['Uranus', 'Neptune', 'Saturn', 'Jupiter'], correct: 0, explanation: 'Uranus rotates on its side with an axial tilt of 98 degrees.', points: 20 }, { id: 8, type: 'true-false', category: 'basic', difficulty: 'easy', question: 'The Sun is a star.', options: ['True', 'False'], correct: 0, explanation: 'True! The Sun is a G-type main-sequence star (yellow dwarf).', points: 10 }, { id: 9, type: 'multiple-choice', category: 'advanced', difficulty: 'medium', question: 'Which planet has the most beautiful ring system?', options: ['Jupiter', 'Saturn', 'Uranus', 'Neptune'], correct: 1, explanation: 'Saturn is famous for its spectacular ring system made of ice and rock particles.', points: 15 }, { id: 10, type: 'multiple-choice', category: 'basic', difficulty: 'easy', question: 'How many planets are in our solar system?', options: ['7', '8', '9', '10'], correct: 1, explanation: 'There are 8 planets in our solar system: Mercury, Venus, Earth, Mars, Jupiter, Saturn, Uranus, and Neptune.', points: 10 } ]; } setupQuizUI() { const quizContainer = document.getElementById('quizContainer'); if (!quizContainer) return; quizContainer.innerHTML = ` <div class="quiz-header"> <h2>Solar System Quiz</h2> <div class="quiz-progress"> <span id="questionCounter">Question 1 of ${this.questions.length}</span> <div class="progress-bar"> <div class="progress-fill" id="quizProgress"></div> </div> </div> </div> <div class="quiz-content"> <div id="questionDisplay"></div> <div id="answerOptions"></div> <div class="quiz-controls"> <button id="prevBtn" class="quiz-btn" disabled>Previous</button> <button id="nextBtn" class="quiz-btn">Next</button> <button id="submitBtn" class="quiz-btn" style="display: none;">Submit Quiz</button> </div> </div> <div id="quizResults" style="display: none;"></div> `; this.displayQuestion(); } displayQuestion() { const question = this.questions[this.currentQuestion]; const questionDisplay = document.getElementById('questionDisplay'); const answerOptions = document.getElementById('answerOptions'); const questionCounter = document.getElementById('questionCounter'); const progressFill = document.getElementById('quizProgress'); // Update progress questionCounter.textContent = `Question ${this.currentQuestion + 1} of ${this.questions.length}`; progressFill.style.width = `${((this.currentQuestion + 1) / this.questions.length) * 100}%`; // Display question questionDisplay.innerHTML = ` <div class="question-card"> <h3>${question.question}</h3> <div class="question-meta"> <span class="difficulty ${question.difficulty}">${question.difficulty}</span> <span class="category">${question.category}</span> <span class="points">${question.points} points</span> </div> </div> `; // Display answer options answerOptions.innerHTML = ''; question.options.forEach((option, index) => { const optionDiv = document.createElement('div'); optionDiv.className = 'answer-option'; optionDiv.innerHTML = ` <input type="radio" name="answer" id="option${index}" value="${index}"> <label for="option${index}">${option}</label> `; answerOptions.appendChild(optionDiv); }); // Update navigation buttons this.updateNavigationButtons(); } updateNavigationButtons() { const prevBtn = document.getElementById('prevBtn'); const nextBtn = document.getElementById('nextBtn'); const submitBtn = document.getElementById('submitBtn'); prevBtn.disabled = this.currentQuestion === 0; if (this.currentQuestion === this.questions.length - 1) { nextBtn.style.display = 'none'; submitBtn.style.display = 'inline-block'; } else { nextBtn.style.display = 'inline-block'; submitBtn.style.display = 'none'; } } bindEvents() { document.addEventListener('click', (e) => { if (e.target.id === 'nextBtn') { this.nextQuestion(); } else if (e.target.id === 'prevBtn') { this.previousQuestion(); } else if (e.target.id === 'submitBtn') { this.submitQuiz(); } }); // Track answer selection document.addEventListener('change', (e) => { if (e.target.name === 'answer') { this.userAnswers[this.currentQuestion] = parseInt(e.target.value); } }); } nextQuestion() { if (this.currentQuestion < this.questions.length - 1) { this.currentQuestion++; this.displayQuestion(); } } previousQuestion() { if (this.currentQuestion > 0) { this.currentQuestion--; this.displayQuestion(); } } submitQuiz() { this.calculateScore(); this.displayResults(); this.saveProgress(); } calculateScore() { this.score = 0; this.userAnswers.forEach((answer, index) => { if (answer === this.questions[index].correct) { this.score += this.questions[index].points; } }); this.quizCompleted = true; } displayResults() { const quizContent = document.querySelector('.quiz-content'); const quizResults = document.getElementById('quizResults'); const totalPoints = this.questions.reduce((sum, q) => sum + q.points, 0); const percentage = Math.round((this.score / totalPoints) * 100); quizContent.style.display = 'none'; quizResults.style.display = 'block'; let grade, message; if (percentage >= 90) { grade = 'A+'; message = 'Excellent! You\'re a solar system expert!'; } else if (percentage >= 80) { grade = 'A'; message = 'Great job! You have excellent knowledge!'; } else if (percentage >= 70) { grade = 'B'; message = 'Good work! You have solid knowledge!'; } else if (percentage >= 60) { grade = 'C'; message = 'Not bad! Keep learning!'; } else { grade = 'D'; message = 'Keep studying the solar system!'; } quizResults.innerHTML = ` <div class="results-card"> <h2>Quiz Results</h2> <div class="score-display"> <div class="score-circle"> <span class="score-number">${this.score}</span> <span class="score-total">/ ${totalPoints}</span> </div> <div class="grade">${grade} (${percentage}%)</div> </div> <p class="result-message">${message}</p> <div class="question-review"> <h3>Question Review</h3> ${this.generateQuestionReview()} </div> <div class="quiz-actions"> <button id="retakeQuiz" class="quiz-btn">Retake Quiz</button> <button id="backToLearning" class="quiz-btn">Back to Learning</button> </div> </div> `; // Bind result actions document.getElementById('retakeQuiz').addEventListener('click', () => { this.resetQuiz(); }); document.getElementById('backToLearning').addEventListener('click', () => { window.location.href = 'index.html'; }); } generateQuestionReview() { let reviewHTML = ''; this.questions.forEach((question, index) => { const userAnswer = this.userAnswers[index]; const isCorrect = userAnswer === question.correct; const userAnswerText = userAnswer !== undefined ? question.options[userAnswer] : 'Not answered'; const correctAnswerText = question.options[question.correct]; reviewHTML += ` <div class="review-item ${isCorrect ? 'correct' : 'incorrect'}"> <div class="review-question"> <strong>Question ${index + 1}:</strong> ${question.question} </div> <div class="review-answers"> <div class="user-answer"> <span class="answer-label">Your answer:</span> ${userAnswerText} </div> <div class="correct-answer"> <span class="answer-label">Correct answer:</span> ${correctAnswerText} </div> </div> <div class="explanation"> <strong>Explanation:</strong> ${question.explanation} </div> </div> `; }); return reviewHTML; } resetQuiz() { this.currentQuestion = 0; this.score = 0; this.userAnswers = []; this.quizCompleted = false; document.querySelector('.quiz-content').style.display = 'block'; document.getElementById('quizResults').style.display = 'none'; this.displayQuestion(); } saveProgress() { const progress = { score: this.score, totalQuestions: this.questions.length, completedAt: new Date().toISOString(), userAnswers: this.userAnswers }; localStorage.setItem('quizProgress', JSON.stringify(progress)); } loadProgress() { const saved = localStorage.getItem('quizProgress'); if (saved) { return JSON.parse(saved); } return null; } } // Initialize quiz system when DOM is ready document.addEventListener('DOMContentLoaded', () => { if (document.getElementById('quizContainer')) { window.quizSystem = new QuizSystem(); } });